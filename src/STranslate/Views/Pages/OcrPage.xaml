<ui:Page
    x:Class="STranslate.Views.Pages.OcrPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cnvt="clr-namespace:STranslate.Converters"
    xmlns:control="clr-namespace:STranslate.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dd="urn:gong-wpf-dragdrop"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:vm="clr-namespace:STranslate.ViewModels.Pages"
    Title="Ocr"
    d:DataContext="{d:DesignInstance Type=vm:OcrViewModel,
                                     IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <ui:Page.Resources>
        <control:ImTranOcrServiceDropHandler x:Key="ImTranOcrDropHandler" />
    </ui:Page.Resources>

    <Grid Margin="20,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock
                DockPanel.Dock="Left"
                Style="{DynamicResource {x:Static ui:ThemeKeys.SubtitleTextBlockStyleKey}}"
                Text="{DynamicResource Ocr}" />
            <Grid HorizontalAlignment="Right" DockPanel.Dock="Right">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.Resources>
                    <Style BasedOn="{StaticResource {x:Type control:IconButton}}" TargetType="control:IconButton">
                        <Setter Property="Padding" Value="5" />
                    </Style>
                    <Style BasedOn="{StaticResource {x:Type Image}}" TargetType="Image">
                        <Setter Property="Height" Value="{DynamicResource STControlFontSize16}" />
                    </Style>
                </Grid.Resources>
                <TextBlock
                    Margin="0,0,4,0"
                    Style="{DynamicResource TextBlockInfoStyle}"
                    Text="&#xF4A3;"
                    ToolTip="{DynamicResource SpecialServiceInfo}" />
                <Grid
                    Grid.Column="1"
                    dd:DragDrop.DropHandler="{StaticResource ImTranOcrDropHandler}"
                    dd:DragDrop.IsDropTarget="True"
                    Background="Transparent">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        Margin="5,0"
                        VerticalAlignment="Center"
                        Foreground="{DynamicResource {x:Static ui:ThemeKeys.TextFillColorSecondaryBrushKey}}">
                        <Run Text="{DynamicResource ImTranOcrService}" />
                        <Run Text=":" />
                    </TextBlock>
                    <ui:ElevationBorder
                        Grid.Column="1"
                        MinWidth="100"
                        MinHeight="30"
                        Padding="8,2"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Background="{DynamicResource SettingsCardBackground}"
                        BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="5"
                        FlowDirection="LeftToRight"
                        Visibility="{Binding Service.ImageTranslateOcrService, Converter={cnvt:EqualToVisibilityInverseConverter}}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="20" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Image Source="{Binding Service.ImageTranslateOcrService.IconPath, Converter={cnvt:ImagePathToSourceConverter}}" />
                            <TextBlock
                                Grid.Column="1"
                                Margin="5,0"
                                VerticalAlignment="Center"
                                FontSize="{DynamicResource STControlFontSize12}"
                                FontWeight="SemiBold"
                                Foreground="{DynamicResource {x:Static ui:ThemeKeys.SystemFillColorSuccessBrushKey}}"
                                Text="{Binding Service.ImageTranslateOcrService.DisplayName}"
                                TextTrimming="CharacterEllipsis"
                                ToolTipService.ToolTip="{Binding Service.ImageTranslateOcrService.DisplayName}" />
                            <control:Badge
                                Grid.Column="2"
                                Margin="2,0"
                                FontSize="{DynamicResource STControlFontSize10}"
                                IsOfficial="{Binding Service.ImageTranslateOcrService.MetaData.IsPrePlugin}" />
                        </Grid>
                    </ui:ElevationBorder>
                    <ui:ElevationBorder
                        Grid.Column="1"
                        MinWidth="100"
                        MinHeight="30"
                        Margin="0,0,5,0"
                        Padding="8,2"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Background="{DynamicResource SettingsCardBackground}"
                        BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="5"
                        Visibility="{Binding Service.ImageTranslateOcrService, Converter={cnvt:EqualToVisibilityConverter}}">
                        <TextBlock
                            Margin="5,0"
                            VerticalAlignment="Center"
                            Foreground="{DynamicResource {x:Static ui:ThemeKeys.TextFillColorSecondaryBrushKey}}"
                            Text="{DynamicResource NoServiceEnabled}" />
                    </ui:ElevationBorder>
                    <control:IconButton
                        Grid.Column="2"
                        Margin="0,0,5,0"
                        Command="{Binding DeactiveImTranOcrCommand}"
                        FontSize="{DynamicResource STControlFontSize8}"
                        Icon="{x:Static ui:FluentSystemIcons.Delete_20_Regular}"
                        Visibility="{Binding Service.ImageTranslateOcrService, Converter={cnvt:EqualToVisibilityInverseConverter}}" />
                </Grid>
            </Grid>
        </DockPanel>
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="200" />
                <ColumnDefinition Width="1" />
                <ColumnDefinition Width="3*" MinWidth="300" />
            </Grid.ColumnDefinitions>
            <Grid>

                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <control:ServicePanel
                    x:Name="ServicePanel"
                    Grid.Row="0"
                    VerticalAlignment="Stretch"
                    dd:DragDrop.IsDragSource="True"
                    dd:DragDrop.IsDropTarget="True"
                    dd:DragDrop.UseDefaultDragAdorner="True"
                    ActiveImTranOcrCommand="{Binding ActiveImTranOcrCommand}"
                    DeleteCommand="{Binding DeleteCommand}"
                    DuplicateCommand="{Binding DuplicateCommand}"
                    ImTranOcrService="{Binding Service.ImageTranslateOcrService}"
                    ItemsSource="{Binding Service.Services}"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    SelectedItem="{Binding SelectedItem, Mode=TwoWay}" />
                <UniformGrid
                    Grid.Row="1"
                    Margin="0,10,20,0"
                    Columns="2">
                    <Button
                        Margin="0,0,5,0"
                        HorizontalAlignment="Stretch"
                        Command="{Binding AddServiceCommand}">
                        <ui:IconAndText Content="{DynamicResource Add}" Icon="{x:Static ui:FluentSystemIcons.Add_20_Regular}" />
                    </Button>
                    <Button
                        Margin="5,0,0,0"
                        HorizontalAlignment="Stretch"
                        Command="{Binding DeleteCommand}"
                        CommandParameter="{Binding SelectedItem}">
                        <ui:IconAndText Content="{DynamicResource Delete}" Icon="{x:Static ui:FluentSystemIcons.Delete_20_Regular}" />
                    </Button>
                </UniformGrid>
            </Grid>
            <GridSplitter
                Grid.Column="1"
                Margin="0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Background="{DynamicResource CardStrokeColorDefaultBrush}"
                ResizeBehavior="PreviousAndNext"
                ResizeDirection="Columns" />
            <Grid Grid.Column="2" Margin="20,0,0,0">
                <control:STranslateBadge Visibility="{Binding SelectedItem, Converter={cnvt:EqualToVisibilityConverter}}" />

                <ui:ScrollViewerEx Margin="0,0,-20,0">
                    <ContentControl Margin="0,0,20,0" Content="{Binding SettingUI}" />
                </ui:ScrollViewerEx>
            </Grid>
        </Grid>
    </Grid>
</ui:Page>
