<UserControl
    x:Class="STranslate.Plugin.Translate.DeepL.View.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
    xmlns:local="clr-namespace:STranslate.Plugin.Translate.DeepL"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:plugin="clr-namespace:STranslate.Plugin;assembly=STranslate.Plugin"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:vm="clr-namespace:STranslate.Plugin.Translate.DeepL.ViewModel"
    d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <ikw:SimpleStackPanel Spacing="12">
        <ui:SettingsCard Description="{DynamicResource STranslate_Plugin_Translate_DeepL_ApiKey_Description}" Header="{DynamicResource STranslate_Plugin_Translate_DeepL_ApiKey}">
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.Key_24_Regular}" />
            </ui:SettingsCard.HeaderIcon>
            <PasswordBox
                MinWidth="300"
                plugin:PasswordBoxAssistant.Attach="True"
                plugin:PasswordBoxAssistant.Password="{Binding ApiKey}"
                ui:ControlHelper.PlaceholderText="{DynamicResource STranslate_Plugin_Translate_DeepL_ApiKey_Placeholder}" />
        </ui:SettingsCard>

        <ui:SettingsCard Description="{DynamicResource STranslate_Plugin_Translate_DeepL_ApiType_Description}" Header="{DynamicResource STranslate_Plugin_Translate_DeepL_ApiType}">
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.PersonStar_20_Regular}" />
            </ui:SettingsCard.HeaderIcon>
            <ikw:SimpleStackPanel MinWidth="300" Spacing="6">
                <ComboBox SelectedValue="{Binding ApiType}" SelectedValuePath="Tag">
                    <ComboBoxItem Content="api" Tag="{x:Static local:DeepLApiType.Api}" />
                    <ComboBoxItem Content="api-free" Tag="{x:Static local:DeepLApiType.ApiFree}" />
                    <ComboBoxItem Content="custom" Tag="{x:Static local:DeepLApiType.Custom}" />
                </ComboBox>
                <TextBlock
                    Text="{Binding ApiUrlPreview}"
                    TextWrapping="Wrap"
                    Visibility="{Binding IsPresetApi, Converter={StaticResource BooleanToVisibilityConverter}}" />
                <TextBox
                    Text="{Binding CustomApiUrl, UpdateSourceTrigger=PropertyChanged}"
                    ui:ControlHelper.PlaceholderText="{DynamicResource STranslate_Plugin_Translate_DeepL_CustomApiUrl_Placeholder}"
                    Visibility="{Binding IsCustomApi, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </ikw:SimpleStackPanel>
        </ui:SettingsCard>

        <ui:SettingsCard Header="{DynamicResource STranslate_Plugin_Translate_DeepL_Usage}">
            <ui:SettingsCard.HeaderIcon>
                <ui:FontIcon Icon="{x:Static ui:FluentSystemIcons.DataUsage_20_Regular}" />
            </ui:SettingsCard.HeaderIcon>

            <ikw:SimpleStackPanel Orientation="Horizontal" Spacing="4">
                <ui:ProgressRing
                    Width="20"
                    Height="20"
                    VerticalAlignment="Center"
                    Background="LightGray"
                    IsIndeterminate="False"
                    Value="{Binding Usage}" />
                <TextBlock VerticalAlignment="Center" Text="{Binding UsageStr}" />

                <Button Command="{Binding QueryUsageCommand}" Content="{DynamicResource STranslate_Plugin_Translate_DeepL_Query}" />
            </ikw:SimpleStackPanel>
        </ui:SettingsCard>

    </ikw:SimpleStackPanel>
</UserControl>
